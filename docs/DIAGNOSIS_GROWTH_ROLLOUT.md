# Диагнозы → повторная игра → регистрация → клуб

Подготовлено 7 сентября 2026. Основание: полный аудит 07.09, а не гипотеза о дефиците SEO. За последние 7 завершённых дней: 72 старта и 54 завершения Диагнозов в общей серверной активности; consented organic cohort — 16 входов / 12 стартов / 6 завершений / 1 следующий старт. Выборка органики мала. За 05–06.09: 17 завершений / 5 связанных переходов в другие игры, в органике 2 / 0. Это baseline, не эффект нового сценария.

## Выпуск и последовательность

Штатный единый release из main (web/API/worker), миграция `0052_diagnosis_growth_funnel`. Никаких новых карточек, AI-генераций, SEO-текстов, цен и прав действующих подписок.

Сразу: Яндекс выше email-формы; выбор тарифа гостем измеряется до регистрации; условия и автопродление не выбраны заранее; параметр `product` больше не запускает checkout автоматически. После входа пользователь подтверждает покупку сам. Сохраняются тариф, непрозрачный intent и allowlisted контекст Диагнозов; после подтверждённой оплаты есть возврат к игре.

Эксперимент управляется в `/admin/economy#growth`, API `GET/PATCH /api/v1/admin/growth`. Setting `growth.diagnosisContinuation`, аудит изменений. По умолчанию `baseline`, автоматического календарного включения нет.

1. **baseline:** сохранить чистое окно рекомендаций 05–11.09. Решение 12.09 по действующему SEO-плану.
2. **replay:** основная кнопка «Следующий случай», видимая стоимость до списания, другие игры вторичны. Не раньше 12.09. Измерять первый завершённый результат игрока в окне → связанный повторный старт → завершение повтора. Операционные ориентиры: 30/100 продолжили, 20/100 завершили вторую; при малой выборке показывать абсолютные числа, не обещать эффект.
3. **registration:** только после оценки минимум 7 полных UTC-дней replay. Новому аккаунту один раз выдаются 3 дополнительные партии Диагнозов. Это не валюта: нельзя потратить на AI, Данетки, другие игры или покупки. Ориентир 10 новых аккаунтов на 100 действительно увиденных предложений — гипотеза, не прогноз.
4. **club:** после отдельного окна регистрации. Контекстное предложение при недостатке билетов или после нескольких игр, 30 дней первичны, год вторичен. Цена берётся из публичного серверного каталога (на baseline 199 ₽ / 30 дней); никаких скидок или включения скрытых ticket bundles.

Админ должен явно отметить, что проверил предыдущий этап. Сервер не разрешает пропуск этапов и переход раньше семи полностью завершённых UTC-дней после включения. Немедленная пауза разрешена. Уже выданные бонусы при паузе сохраняются; активный клуб не расходует бонус.

## Источники истины и ограничения

### Доработка 11 сентября: бесплатное продолжение и точность измерения

- Главный CTA этапа `replay` сначала выбирает непройденный бесплатный день из существующего недельного архива. Права гостя и цены не расширяются. Если таких дней нет, остаются прежние явно показанные способы продолжить. Ошибка загрузки архива блокирует CTA и предлагает повторить запрос, а не подменяет бесплатную игру платной.
- Архивный старт принимает `sourceSessionId`: сервер проверяет владельца, завершённую публичную партию «Диагнозов», другую дату и действующее бесплатное окно. Сохраняет `access_source=free_archive`, связь с исходной партией и текущий этап. Кошелёк и бонусы не списываются. Уже начатые сессии не перепривязываются и не выдаются за новые.
- До ручной проверки завершённых 5–11 сентября CTA остаётся выключенным. Сам наступивший день не активирует этап; при включении 12 сентября первая полная неделя — 13–19 сентября, проверка 20 сентября.
- `measurement.from` остаётся 8 сентября: это начало предыдущей серверной связности, не начало бесплатного архива. Версия `linked-repeat-v2-free-archive` и первая фактическая запись `freeArchiveFirstObservedAt` отделяют новую возможность; до неё — `null`. Разбивка повторов по способу доступа использует прежнюю связанную когорту; людей между способами доступа суммировать нельзя.
- Показ регистрации теперь означает видимость не менее 25% в активной вкладке, не mount. Версия `visible-v2`: исторические показы с ней несопоставимы напрямую. Новые `result_registration_offer_view/clicked` дополняют, а не переименовывают существующий `result_save_progress`. Каталог целей Метрики проверять отдельно: отправка события не создаёт цель.
- Приглашения ведут на публичный URL игры, без чужого session ID и sender ID. `challenge_opened/accepted` — открытие/принятие приглашения получателем; `challenge_started` — подтверждённое сервером открытие собственной играбельной сессии (ready/resumed), не первая попытка. `challenge_completed` — её серверный финал. Старые уже завершённые партии показывают сравнение, но не новое завершение.
- Три UTM slug-поля передаются только при согласии на аналитику и проходят через OAuth до серверного `sign_up`. Персональные поля, произвольный query string и секреты не сохраняются. Рекламная когорта отдельно от органики: `/admin/economy#campaigns`, методика и пакет материалов в [GROWTH_ACQUISITION_PILOT.md](GROWTH_ACQUISITION_PILOT.md). Измеренные acquisition IDs не равны людям или визитам Метрики; отказавшиеся не превращаются в нулевую конверсию.

### Основные определения

- `registration_play_credits`: одна строка на новый аккаунт, remaining 0–3; grant вызывается при фактическом создании неанонимного user. Повторный вход не вызывает начисление.
- Списание бонуса, запуск игры и обновление кошелька/usage — одна транзакция. Повтор одного idempotency key возвращает ту же сессию; параллельные запросы не дают четвёртой бонусной партии. Бонус не повышает платную дневную лестницу.
- `game_sessions.access_source`, `source_session_id`, `growth_stage`: серверные источники повторов и использования льготы. Source session проверяется на владельца, тот же режим, завершение и отсутствие спецподборки. Старые NULL не backfill и не считать нулём поведения.
- `completions` — серверные завершения по completedAt (в том числе проигрыш/раскрытие/истечение), не подтверждённые клиентские просмотры результата. `firstCompleters` — игроки с таким завершением в полном выбранном окне; это другая когорта, чем сессии, начатые в окне. `measuredCompleters` — игроки с первым завершением в измеренной части; `repeatingCompleters` — те, кто продолжил после этой первой сессии. Это не все повторные посетители проекта. `repeatStarts/repeatCompletions` связаны именно с ней.
- Первый полный UTC-день нового измерения — 08.09.2026. API возвращает `measurement.from/coverage` (`not_started/partial/complete`); если окно заканчивается до первого полного дня, новые показатели повторов/использования клуба равны `null`, UI показывает «—». В частично покрытом окне первые завершения для повторной когорты выбираются только из измеренной части, не из ранней истории. `complete` означает календарное покрытие, не 100% обновление старых клиентских вкладок. Старты со старого frontend без sourceSessionId и просмотры без согласия отдельно проверять в аудите.
- Бонус: `bonusGranted/bonusPlayers` — когорта выдачи; `bonusStarts/bonusCompletions` — когорта запусков. Не делить разные когорты друг на друга.
- `accounts.created` — users.createdAt без анонимов/админов; signUps — серверная регистрация тех же аккаунтов, не sign_in и не legacy auth_success.
- Покупки — payment_orders, не клиентский purchase_succeeded; когорта по дате создания заказа. `paidUsersUsedClub` — игроки, использовавшие свободную игру после оплаты. Архив и AI-льготы в этот показатель не входят. Администраторы и stub исключены, другие тестовые покупки требуют ручной классификации. Нет автоматического вывода «первая реальная покупка».
- Новые client events перечислены в таблице ниже. Показ — элемент, видимый минимум на 25% (IntersectionObserver), не mount страницы; first-party eventId стабилен для session + stage.
- События проходят существующую модель согласия, first-touch не сохраняется при отказе. Админская таблица разделяет stage/consent. Метрика получает соответствующие reachGoal; наличие целей в live-каталоге подтверждать отдельно, не считать добавление кода созданием цели.
- intent_id связывает выбор тарифа гостем и авторизованный checkout; metadata заказа хранит intentId, placement, sourceMode. Это не деньги и не источник first-touch. Для атрибуции регистрации использовать consented acquisition/auth_events, не угадывать канал по контексту предложения.

## Цели Метрики

Созданы и повторно проверены 07.09.2026 в авторизованном интерфейсе счётчика `110517987`. Тип — целевое JS-событие, условие «Совпадает», без статического дохода. Существующие цели не изменялись. Это начало нового измерения, а не восстановленная история. Фактическое достижение каждой gated-цели проверять после включения её этапа; наличие в каталоге не доказывает доставку события.

| Событие | ID цели |
|---|---:|
| `diagnosis_replay_offer_view` | 609800202 |
| `diagnosis_replay_clicked` | 609800430 |
| `registration_bonus_offer_view` | 609800561 |
| `registration_bonus_offer_clicked` | 609800808 |
| `club_context_offer_view` | 609800814 |
| `club_context_offer_clicked` | 609800934 |
| `commerce_plan_selected` | 609801406 |

11.09.2026 в том же счётчике созданы и повторно проверены шесть дополнительных целей. Условие — точное совпадение идентификатора, статический доход не задан. История начинается с доставки событий нового релиза, не с создания цели.

| Событие | ID цели |
|---|---:|
| `result_registration_offer_view` | 611808074 |
| `result_registration_offer_clicked` | 611808331 |
| `challenge_opened` | 611808371 |
| `challenge_accepted` | 611808378 |
| `challenge_started` | 611808383 |
| `challenge_completed` | 611808425 |

## Проверка

- Целевые unit и интеграционные проверки с изолированным PostgreSQL, без Docker и production-данных.
- Проверяются одноразовый grant, запрет гостевого grant, guest merge с сохранением результата, конкурентный idempotency, ограничение режима/количества, сохранность бонусов при паузе, server-only привязка повтора, агрегаты без PII.
- Dev fixture `/dev/growth-preview.html` — реальные React-компоненты с изолированным fetch; не входит в production build. Проверены широкий и узкий экран, продолжение, цена, unchecked renewal, отсутствие автоматического заказа при `?product=` после входа. Реальный платёж не совершался.
- После релиза: web/API SHA совпадают, migration применена, active revision и количества карточек прежние; authenticated `/admin/economy#growth` отвечает, public meta.stage = baseline; штатный smoke зелёный.
- Технический контроль 24–48 часов по завершённому дню. Эффект роста обсуждать только после активации каждого этапа и накопления его отдельного окна; достаточность выборки — по SEO-плану.
