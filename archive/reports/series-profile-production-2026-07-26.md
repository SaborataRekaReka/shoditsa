# Обогащение production-каталога сериалов — 2026-07-26

## Результат

- Активная ревизия: `338fcacc-1b79-4e0c-b886-2b12d6357701`
- Версия: `20260726T054856428Z-series-profile-3d902f73`
- Контрольная сумма: `3d902f73671c5cf9fc31c62577d7a12a0a1123f4abe4533f144e470327b4d4a0`
- Карточек сериалов: 791
- Карточек, изменённых по сохранённому плану: 790
- Ошибок API: 0

Изменённые поля:

| Поле | Карточек |
| --- | ---: |
| `episodes` | 790 |
| `seasonsCount` | 116 |
| `seriesStatus` | 35 |
| `slogan` | 46 |
| `supportingCast` | 5 |
| `backdropUrl` | 2 |
| `endYear` | 1 |

## Что осталось незаполненным

- `episodes`: 1 — `kp_9167314`, «Чудовище внутри меня». Endpoint сезонов Кинопоиска не вернул положительное число эпизодов.
- `slogan`: 379. В ответе Кинопоиска слогана нет.
- `backdropUrl`: 229. В ответе Кинопоиска обложки нет.
- `supportingCast`: 10. Staff endpoint не дал достаточного набора актёров для позиций 6–10.
- `endYear`: отсутствует у 127 сериалов; это ровно 127 сериалов со статусом «Еще выходит», поэтому это не дефект заполнения.

`seasonsCount` и `seriesStatus` заполнены у всех 791 сериалов.

## Использование API

Выполнено 1 597 рабочих запросов через пять ключей: 320, 320, 319, 319 и 319. Повторных запросов из-за ошибок не было.

До рабочего прогона дневной расход уже составлял 159, 158, 158, 158 и 158 запросов. После прогона расчётный дневной остаток: 21, 22, 23, 23 и 23 запроса. Ключи и секреты в отчёты не записывались. Повторные активации выполнялись по сохранённому плану и к API не обращались.

## Резервная копия

- Файл: `/opt/shoditsa/backups/pre-series-profile-enrichment-20260726T053615Z.dump`
- Размер: 407 МБ
- SHA-256: `56edf5f967a8ad00d378e162a6c16ac432a2a70f145e7363f6ac94eff0d34a23`
- Формат: PostgreSQL custom archive
- Проверка: SHA-256 совпала, `pg_restore --list` прочитал архив (472 TOC-записи).

## Контроль применения

Первая кандидатная ревизия `5faa0e2a-cb99-4cf7-bff0-37f1ef4890f2` была немедленно откатана и помечена `failed`: постпроверка обнаружила непреднамеренный пересчёт флага `allowed_in_game` у пяти «данеток». Клонирование исправлено так, чтобы все режимы вне `series` копировались без изменений.

Для финальной ревизии подтверждено:

- ровно одна активная ревизия и корректный `active_content_revision_id`;
- 5 994 элемента, 19 312 алиасов и 960 диагностических виньеток — столько же, сколько до операции;
- ноль отличий во всех не-сериальных карточках;
- ноль отличий в несвязанных полях сериалов;
- точное совпадение количества изменений по каждому полю с планом;
- все восемь режимов сохранили исходные размеры и флаги допуска;
- catalog repair dry-run: 0 исправлений, 0 конфликтных групп, 0 изменений игровых пулов;
- `/api/v1/health/live`, `/api/v1/health/ready` и `/api/v1/meta`: HTTP 200;
- локальная библиотека: 791 карточка, 0 расхождений с планом;
- `data:validate`: 0 ошибок и 0 карантинных записей;
- 5 профильных unit-тестов и 45 тестов game-core/admin-content прошли.
