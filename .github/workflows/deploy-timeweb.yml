name: Deploy To Timeweb

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

concurrency:
  group: deploy-timeweb-main
  cancel-in-progress: true

permissions:
  contents: read

env:
  DEPLOY_HOST: 72.56.240.222
  DEPLOY_USER: root
  DEPLOY_PORT: 22
  DEPLOY_TARGET_DIR: /opt/repeto/deploy/shoditsa

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Validate music runtime files
        run: |
          test -f dist/data/libraries/music/items.json
          test -f dist/data/libraries/music/search-index.json
          test -f dist/data/music.generated.json

      - name: Validate SSH secret exists
        run: |
          if [ -z "${{ secrets.TIMEWEB_SSH_KEY }}" ]; then
            echo "::error::TIMEWEB_SSH_KEY is missing or empty"
            exit 1
          fi

      - name: Configure SSH key
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s' "${{ secrets.TIMEWEB_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p "$DEPLOY_PORT" "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Clean target directory
        run: |
          set -euo pipefail
          ssh -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" \
            "mkdir -p $DEPLOY_TARGET_DIR; find $DEPLOY_TARGET_DIR -mindepth 1 -maxdepth 1 -exec rm -rf {} +"

      - name: Upload dist to server
        run: |
          set -euo pipefail
          scp -P "$DEPLOY_PORT" -r dist/* "$DEPLOY_USER@$DEPLOY_HOST:$DEPLOY_TARGET_DIR"

      - name: Set permissions and list deployed files
        run: |
          set -euo pipefail
          ssh -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" \
            "chmod -R a+rX $DEPLOY_TARGET_DIR; ls -la $DEPLOY_TARGET_DIR | head -n 20"
